{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the main ShellDesk window to use Tauri core window/event APIs and updater APIs.",
  "windows": [
    "main",
    "connection-*"
  ],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
